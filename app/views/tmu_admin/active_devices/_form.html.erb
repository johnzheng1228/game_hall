<%= form_for(@active_device) do |f| %>
  <% if @active_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@active_device.errors.count, "error") %> prohibited this active_device from being saved:</h2>

      <ul>
      <% @active_device.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device %><br>
    <%= f.text_field :device %>
  </div>
  <div class="field">
    <%= f.label :serail %><br>
    <%= f.text_field :serail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
